# Good News! Classifier

## Train Classifiers
Run
```bash
python classifier.py
```

## Fetch and Analyse Headlines
Run
```bash
python headline-analyser.py --<print|persist>
```

Or, this can be acheived with docker:

```bash
docker-compose up analyser
```